<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Alert Mail</title>
    <style>
      table {
        font-family: arial, sans-serif;
        border-collapse: collapse;
        width: 100%;
      }
      td,
      th {
        border: 1px solid #dddddd;
        text-align: left;
        padding: 8px;
      }
      .danger {
        background-color: red;
      }
      .success {
        background-color: green;
      }
    </style>
  </head>
  <body>
    <table>
      <tr>
        <th>Services</th>
        <th>Status</th>
      </tr>
      <% serviceArr.forEach(function(service) {%> <%
      if(service?.value?.serviceName){ %>
      <tr
        class="<%= service.value.status === 'Running' ? 'success' : 'danger' %>"
      >
        <td><%= service.value.serviceName %></td>
        <td><%= service.value.status %></td>
      </tr>
      <% } %> <% }); %>
    </table>

    <ul>
      <li>CPU Count : <%= serviceArr[serviceArr.length-1].value.cpuCount %></li>
      <li>Total Memory : <%= serviceArr[serviceArr.length-1].value.totalMemory %></li>
      <li>Free Memory : <%= serviceArr[serviceArr.length-1].value.freeMemory %></li>
      <li>Up Time : <%= serviceArr[serviceArr.length-1].value.upTime %></li>
    </ul>
  </body>
</html>
